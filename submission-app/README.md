# Submission App (React)

Web form for employees to submit their weekly work hours.

## Folder Structure
- `components/`: UI components
- `api/`: API calls
- `types/`: TypeScript types
- `styles/`: CSS styles
- `assets/`: logo image

## Run the app
1. Install dependencies: `npm install`
2. Start the development server: `npm start`
